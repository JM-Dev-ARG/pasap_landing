---
interface Props {
  nombre: string;
  description: string;
  imagen?: string;
  bg_color: string;
  id: number;
}

const { nombre, description, imagen, bg_color, id } = Astro.props;
---

<div class="flex flex-col items-center justify-center gap-4 w-full">
  <div
    class="w-[60%] lg:w-full h-full bg-contain bg-no-repeat bg-center flex items-center justify-center aspect-square"
    style={`background-image: url(${imagen})`}
  >
    <p
      class=" text-[clamp(4.75rem,3vw,5.5rem)] font-semibold text-gray-50 font-title py-10"
    >
      {id}
    </p>
  </div>
  <div
    class="w-[60%] lg:w-full text-gray-50 text-2xl pl-4 py-2 rounded-lg"
    style={`background-color: ${bg_color}`}
  >
    {nombre}
  </div>
  <p class=" w-[60%] lg:w-full text-[clamp(1rem,3vw,1.5rem)] text-gray-900 text-left text-pretty">
    {description}
  </p>
</div>
